import math
import graphics


==make the window
window = graphics.newWindow(500, 500, "Window test")

==draw a circle
graphics.drawCircle(window, 100, 100, 50, "red")

==use pause() so that the window can be seen (stops the program from automatically closing)
pause()